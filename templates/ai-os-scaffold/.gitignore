.obsidian/workspace.json
.obsidian/workspace-mobile.json
.DS_Store
.trash/

# _dump/ — RETIRED as an OS convention (2026-09-03). Kept as a one-line guard only, so a
# stray scratch drop can never be committed. It is no longer part of the vault structure.
_dump/
